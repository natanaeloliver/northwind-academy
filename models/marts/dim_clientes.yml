version: 2
models:
  - name: dim_clientes
    description: >
      Tabela dimensão dos clientes da Northwind.
    columns:
      - name: PK_CLIENTE
        description: Chave primária da dimensão de clientes.
        tests:
          - unique
          - not_null









